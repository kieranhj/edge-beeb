# Decisions of record

The table every deviation from the C64 original, and every choice the hardware forced, gets a row in.
Per-layer detail lives in that layer's `docs/layer-*.md`; this is the index. Nothing here is
re-litigated without a new row saying why.

| # | Date | Decision | Where |
|---|---|---|---|
| 1 | 2026-09-02 | **Target is the BBC Master 128 only.** The 1-pixel scroll needs two full 16K screens inside a hardware wrap region (main and shadow RAM at `&4000-&7FFF`), which a Model B cannot provide. Paradroid costed sub-4-pixel scrolling on a B and rejected every option. A B+128 has shadow RAM with different paging and is not planned | `PROPOSAL.md` §2 |
| 2 | 2026-09-02 | **Sprites are clipped at the play-area edges, not culled.** Enemies enter from the right and the bullet leaves it; Paradroid's whole-sprite cull would make them pop. Edge culling is the fallback only if clipping cannot fit the frame budget | `PROPOSAL.md` §3.7 |
| 3 | 2026-09-02 | **Hand-authored MODE 2 art is a free redraw inside the existing tile boundaries.** All eight colours, gradients and fat-pixel dithering; tile outlines, the 4 × 4 character grid, the 211 tile definitions and the map stay the C64's. `reference/beeb-artwork-example.jpg` is the target look | `PROPOSAL.md` §5.2 |
| 4 | 2026-09-02 | **Art is exchanged by shared folder or email**, mirrored into `assets/` by a script; we commit. No git on the artist's side | `PROPOSAL.md` §5.2 |
| 5 | 2026-09-02 | **Music is converted automatically from the CPC Arkos tune** with the nova-invite toolchain (`Repos/nova-invite/bin`: Arkos Tracker 2 → `SongToYm.exe` → `ym2sn.py` → `vgmpacker.py`, played by `lib/vgcplayer.asm`). Not a commission | `PROPOSAL.md` §7 |
| 6 | 2026-09-02 | **Keep the 2019 scroll scheme**: two phase-offset shadow screens, one byte column per frame through a 160-byte column buffer. It is the CPC's tile writer and it works at 1 px / 25 Hz. Paradroid's tranche split and window A/B scheduling are *not* adopted; they exist only because Paradroid is single-buffered | `PROPOSAL.md` §1, §3 |
| 7 | 2026-09-02 | **Sprite engine v2 follows Paradroid's model**: slot table, save area mirroring screen geometry, mask derived from the data byte (transparent = logical 0, black inside sprites = logical 8), two pre-shifted copies, deferred-carry `SCANSTEP`, restore replays the draw. Only the player and bullet are compiled; enemies are interpreted with bounding boxes | `PROPOSAL.md` §3 |
| 8 | 2026-09-02 | **The status bar is a CRTC rupture** (panel cycle at a fixed address below `&3000`, play cycle at the scrolling address), lifted from Paradroid's `rupture.asm` with its register-window rules. Not MODE 7 mixing | `PROPOSAL.md` §4 |
| 9 | 2026-09-02 | **Layer 0 keeps the binary identical.** The source split, build script and `!BOOT` change nothing in `Edge` or `BANK0`; verified by extracting both files from the old and new disc images | `docs/layer-0-toolchain.md` |
| 10 | 2026-09-02 | **`bin/` is gone.** `convert_sprites.py` was an unfinished stub (syntax errors) that the old `make.bat` ran and ignored; `bbc.py`, `png.py` and `png2bbc.py` were not part of the build. New tools go in `tools/` and use Pillow. The old files are in git history (commit `53af281`) and `bbc.py`'s MODE 2 packing survives in `Repos/nova-invite/bin` | `docs/layer-0-toolchain.md` |
| 11 | 2026-09-02 | **C64 → BBC colour table for the mechanical conversion** (`tools/bbc.py` `C64_TO_BBC`): the eight saturated colours map to themselves; brown (the playfield's shared `$d022`) → blue as the 2019 port chose; orange → red, pink → magenta, dark grey → blue, light green → green, light blue → cyan, greys → white. For the mechanical pass only; the artist's redraw replaces it | `docs/layer-1-graphics-pipeline.md` |
| 12 | 2026-09-02 | **Characters are stored as four MODE 2 column planes** (8K) rather than plain 4 bpp, so the column buffer ORs a plane byte straight in. That fills bank 0, so sprite data lives in bank 1 (slot 5) and `plot_sprite` pages it in; bank 0 is the resting state | `docs/layer-1-graphics-pipeline.md` |
| 13 | 2026-09-02 | **The sprite plotter keeps reading raw C64 data until Layer 3.** `sprites.bin` is exported now (format may change) but rewriting the 2019 plotter for it, then again for the Layer 3 engine, was not worth it | `docs/layer-1-graphics-pipeline.md` |
| 14 | 2026-09-02 | **The map wraps at its true end (302 columns)** rather than at 256, in `map_read`. The C64 never reaches the end: its wave table raises the completion flag first, which Layer 5 will port; the wrap is only what happens until then | `docs/layer-2-display.md` |
| 15 | 2026-09-02 | **Two rupture cycles, not Paradroid's three**: panel (5 rows, the C64 height) then a 34-row cycle holding the play area and the VSync at the MODE 2 row. No R5/R8 because nothing scrolls vertically; R7 is a constant | `docs/layer-2-display.md` |
| 16 | 2026-09-02 | **The VSync IRQ owns the bank flip**: the loop parks the scroll address and a ready flag; the handler flips both shadow bits and takes the address only when `FRAME_LOCK` = 2 fields have passed. A 25 Hz floor that degrades by whole fields without tearing | `docs/layer-2-display.md` |
| 17 | 2026-09-02 | **The panel lives inside the shadow-switched region (`&3000`) and is drawn into both banks.** At `&2000` jsbeeb displayed it under both shadow states but b-em showed garbage on alternate frames: what the video fetches below `&3000` with the D bit set is emulator-dependent and unverified on hardware, so nothing displayed may live there. Cost: every HUD write goes to both banks. `&2000-&2FFF` becomes the Layer 3 sprite-save area instead | `docs/layer-2-display.md` |
| 18 | 2026-09-03 | **Each pixel shift gets its own 16K sprite bank** (slots 5 and 6), identical in layout, rather than one bank holding both. Both shifts plus the tables come to ~24K. The engine pages in the bank a sprite's x asks for, so every address in the blitter is the same either way, and bank 0 stays the resting state | `docs/layer-3-sprites.md` |
| 19 | 2026-09-03 | **Sprites are drawn by an interpreted, bounding-boxed blitter; nothing is compiled yet.** `PROPOSAL.md` §3.6 proposed compiling the player and bullet. Measured, eight interpreted sprites cost 49,236 of the 79,872-cycle frame and leave room for the game, so compilation waits until Layers 4 and 5 show the real load. **The premise is now in doubt**: that measurement was taken while `BUGS.md` #8 was skipping every sprite past x = 140, and `BUGS.md` #9 has the frame overrunning in play. Not yet re-litigated - a new row goes here if it is | `docs/layer-3-sprites.md` |
| 20 | 2026-09-03 | **The hit flash is a recolour table on the ORA site**, not a second inner loop and not stored flash frames. Eight patch sites repointed when the colour changes, so it costs nothing per byte. It assumes a sprite holds transparent, blue, white and one other colour, which the exporter asserts across all 119 C64 frames; hand-drawn art with more colours needs a table per colour | `docs/layer-3-sprites.md` |
| 21 | 2026-09-03 | **A sprite that straddles the end of the 16K buffer walks `bufp` per column, for the whole sprite.** A buffer row is 640 bytes and 16,384/640 = 25.6, so the wrap point is not row-aligned and a row's seven columns can stop being 8 bytes apart. One conservative test per sprite rather than Paradroid's per-row test: it catches about one sprite in eight and is cheaper than testing 21 rows on all of them | `docs/layer-3-sprites.md` |
| 22 | 2026-09-03 | **Sprites do NOT take the scroll's half-byte bank phase** (`SPR_PHASE_MASK = 0`), reversing the assumption in `PROPOSAL.md` §3.1. Both banks are drawn at the same origin and displayed at the same CRTC address, so a screen-space sprite stands still under both without an adjustment. **Unconfirmed on a display**: if a stationary sprite shimmers one pixel at 50 Hz the mask goes to 1 | `docs/layer-3-sprites.md` |
| 23 | 2026-09-03 | **The game logic ticks twice per display frame.** The C64's main_loop runs at 50 Hz on `sync_wait` and ours at 25, so one pass is two of its iterations. Ticking twice rather than doubling every per-frame constant keeps the original's numbers - 1 of x, 2 of y, 12 of bullet, the `scroll_x AND 3` grind gate - transcribing unaltered, and checks the bullet at each 12-pixel step, which is what makes its three-cell check airtight. ~1,600 cycles a frame | `docs/layer-4-player.md` |
| 24 | 2026-09-03 | **Background collision reads a character map we keep ourselves**, at `&04A0` in main RAM (KC). The C64 reads character codes back out of the screen it displays; we draw pixels, so `plot_char_y` files the code it already has in Y and `player.asm` reads that. 40 x 20, columns as a ring, same grid and same `col_decode` fatal nibble as the original. `&04A0-&07FF` is the language workspace: verified by a sentinel surviving 3,000 fields of the running game | `docs/layer-4-player.md` |
| 25 | 2026-09-03 | **`bullet_colls` guards two cases the original does not.** With no bullet its y is 0 and the C64 indexes 27 rows into a 24-row table; past column 39 it reads into the next row down. Off-screen nonsense there, an out-of-range read here, so both return "no collision" | `docs/layer-4-player.md` |
| 26 | 2026-09-03 | **The keys are Z/X left/right, K/M up/down, L fire** (KC). The C64 uses a joystick; `read_joystick` packs the five into a byte with `$dc00`'s bit order and polarity, so `player_manage` transcribes verbatim. Every internal key number is **measured** with OSBYTE 121 in a BASIC session holding the key, never recalled - Z 97, X 66, K 70, M 101, L 86 | `docs/layer-4-player.md` |
| 27 | 2026-09-03 | **The game-state block lives at `&0800`, not in the code image.** The C64 keeps the same block in its tape buffer at `$0340` for the same reason: RAM that needs no initial value costs nothing to put where the image is not. `&0800-&0BFF` is the MOS's sound, serial and soft-key workspace, ours with the MOS interrupt gone - verified by a sentinel across all four pages surviving 1,500 fields of the running game. Declared after the SAVEs and outside them, so none of it reaches the disc | `docs/layer-5-enemies.md` |
| 28 | 2026-09-03 | **The wave table and `anim_decode` are exported to bank 0**, not hand-copied into the image. The original writes them with symbolic constants (`left_1+up_1`, `enemy_10`), so `tools/export_waves.py` resolves them out of the source; 1,810 bytes of waves and 38 of `anim_decode`. `wave_read` is self-modifying code over the bank exactly as `map_read` is over the map, and bank 0 is the resting state the wave manager already runs under | `docs/layer-5-enemies.md` |
| 29 | 2026-09-03 | **Frames worth it are compiled to straight-line 6502, in sideways slot 7** (KC), partially reversing decision 19. Slot 7 is the fourth of the Master's four sideways RAM banks and was unused - measured, not recalled: a pattern written there survives a page out and back, and the same write to slot 3 vanishes. A compiled body reads no sprite data, since every pixel is an immediate, so it belongs to neither pixel shift and both live in slot 7 together; that also gives the restore one bank to page in and nothing to remember about which shift drew the sprite. Only frames that never hit-flash qualify, because the colours are baked in where the interpreted path reads them through a swappable table, and a compiled body is used only when the sprite is wholly unclipped and no row crosses the end of the buffer - everything else takes the interpreted path untouched. **The player bullet first**: 15 x 6 bytes of box holding 40 opaque, the worst ratio in the game, one frame, no flash. `tools/compile_sprites.py` | `docs/layer-6a-frame-budget.md` |
| 30 | 2026-09-03 | **`DEBUG_COLL` becomes invulnerability, and defaults to 0 in a DEV build.** It used to make a fatal hit flash the player instead of killing him, which is precisely what Layer 6b replaces; what it does now is the C64 source's own `jmp life_lost_init ; patch me out to disable collisions!`, patched out. Off by default even in DEV, because dying is the normal case and the layer cannot be tested with it on - the first `DEBUG_` flag that is not simply `DEV` | `docs/layer-6b-life-cycle.md` |
| 31 | 2026-09-03 | **At zero lives the game re-inits in place instead of returning to a title screen** (KC), because there is no title screen until Layer 6e. The C64's `game_over_loop` ends in a jump to `master_loop`; ours ends by setting `restart_req`, which the **top of the main loop** reads and answers with `game_init` - the old boot sequence lifted into a routine and shared with boot. It must be there and not inside `game_tick`: `scroll_prewind` flips `&FE34` itself, and only at the top of the loop is `frame_ready` reliably 0, so the VSync handler will not flip underneath it. Layer 6c replaces the destination, not the mechanism | `docs/layer-6b-life-cycle.md` |
| 32 | 2026-09-03 | **P pauses and ESCAPE aborts** (KC). The C64 pauses on a keyboard-matrix read this port cannot identify and aborts on Q; P is the BBC convention and neither collides with Z/X/K/M/L. Both internal key numbers **measured** with OSBYTE 121 in a BASIC session holding the key, never recalled - P is 55, ESCAPE is 112, and `*FX229,1` is needed to measure ESCAPE or BASIC eats it. ESCAPE is an ordinary key here because the MOS interrupt is gone and the keyboard is read straight off the VIA. Abort works only from inside the pause, as the original's Q does, and is `main_abort` exactly: one life left and then lose it, so the game-over sequence is the only way a game ends | `docs/layer-6c-state-machine.md` |
| 33 | 2026-09-03 | **The completion finale reads its explosion positions out of our own code.** The C64 reads `$0900` and `$0a00`, which are not tables: it loads at `$0812`, so it is reading its own machine code as a source of scattered numbers. Two pages of ours do the same job and the arithmetic around them is the original's, unaltered. The map was tried first and is useless - the level opens on empty tiles, so every bang landed at x = 0 - and the character set is worse, being MODE 2 pixel patterns with four distinct values in a page | `docs/layer-6c-state-machine.md` |
| 34 | 2026-09-03 | **The status panel is rendered whole, at boot, from a 3,200-byte MODE 2 image, and its C64 colours map faithfully-as-possible** (KC). The original's status bar is a fixed 5 x 40 character map assembled straight into its screen buffer, in the multicolour STATUS charset - four double-width pixels a character, which is one of our 4-fat-pixel cells, so 40 characters is our 160 pixels and the layout transcribes at 1:1 with no rescaling, exactly as the credits page did. `tools/export_panel.py` renders every cell once with its own colour-RAM byte applied, so `panel_init` is a straight copy and the runtime never decodes a character. The C64's palette there is black, blue (`$d022`), white (`$d023`) and per-character dark grey, light grey or light green; MODE 2 has no greys, so dark grey goes to blue and light grey to white - the colours the surrounding bit pairs already use - and light green keeps its hue. The one thing lost is the distinction between dark grey and blue. **The HUD's thirteen glyphs are the exception**: a digit is bit pair 3 for its body with a single pair-2 highlight and a single pair-1 shadow, so with dark grey AND blue both landing on blue a four-pixel-wide digit came out almost entirely blue on black - legible on a 16-colour screen, not on this one (KC: "a bit dark and hard to read"). Their body is white instead; the shadow pixel stays blue, so the shape the artist drew survives. The panel's own artwork is untouched | `docs/layer-6d-hud.md` |
| 35 | 2026-09-04 | **The music player is the VGI one, not the VGC one** (KC). `lib/vgiplayer.asm` from `Repos/vgm-player-bbc`, taken unaltered. The choice is about the shape of the cost: the VGC player's RLE+LZ4 is cheap on most fields and spikes to 5,321 cycles when several streams refill at once, where VGI drops the RLE and decodes exactly one byte per register stream per frame, so its cost cannot scale with a match length - measured worst case 2,674 cycles a field against VGC's 5,321. A frame already at 90% budgets for the worst case, not the mean. The price is size: `.vgi` is about 1.4x `.vgc` and the workspace is 11 x 256 rather than 8 x 256, which is why the tune does not fit. `VGI_UNROLL` is set in `main.asm` rather than passed on the command line, so a bare beebasm invocation cannot get it wrong | `docs/layer-7-music.md` |
| 36 | 2026-09-04 | **The music player and its workspace live in HAZEL, and the tune spans the top of sideways bank 3 and the bottom of HAZEL as one contiguous block** (KC suggested ANDY or HAZEL). Main RAM below `&2000` had 53 bytes and all four sideways banks were spoken for. HAZEL rather than ANDY because it does not overlap the sideways window: the IRQ fires with whatever bank the interrupted code had paged and the sprite engine pages 5, 6 and 7 as it draws, so the handler has to select what the player needs and put `&FE30` back from `&F4` afterwards - and ANDY would have hidden the bank the tune is in. The two halves join because **bank 3 and HAZEL are visible at the same time**, being paged by different registers over different windows, so a pointer walking off `&BFFF` lands in `&C000` and the player never knows; `tools/export_music.py` pads the low half to exactly `MUSIC_LO_SIZE` so the join holds whatever the tune's length. Safe because the MOS's IRQ entry is at `&E59E`, **read out of the vector at `&FFFE` on this machine, not recalled**, which is above HAZEL. HAZEL's resident content is the filing system's workspace, so `MUSIC` is the LAST of the five files loaded and nothing may touch the disc afterwards; OSFILE cannot write there, so it stages through `&4000` like a bank and `move_pages` copies it up with the Y bit set. **And BREAK must clear memory** (KC): a soft BREAK out of the game otherwise leaves the wrecked workspace in place and the machine comes back with no filing system - measured, no DFS banner and `*CAT` returning nothing. `OSBYTE 200` with X = 3 is the first thing `main` does | `docs/layer-7-music.md` |
| 37 | 2026-09-04 | **The tune ships truncated to 203 of its 349 seconds, looped, pending KC.** EDGEA packs to 23,514 bytes of `.vgi` and there are 13,562 between the top of bank 3 and the player in HAZEL. `tools/export_music.py` therefore takes a frame limit, truncating at the YM stage so the VGM the packer sees is a complete register log, and bisects on it for a byte budget; `vgm_init` is called with C = 1 so the player loops what it gets. **This is a placeholder, not a design.** The layer doc costs the ways out: move the panel image out of bank 3 to a boot-time load, which is 71% of the tune for no format work; scatter the format's eleven independent register streams into ANDY and the bank scraps as well, which fits all of it; or cut the tune musically, which only its author can do well. **Superseded by decision 48**, which took the second of those | `docs/layer-7-music.md` |
| 38 | 2026-09-04 | **A loading screen goes up before the banks load, and every data file on the disc ships ZX0-compressed** (KC). `assets/TitlescreenBig.png` is already a MODE 2 screen - 640 x 512 with every 4 x 2 block flat, which `tools/export_loading.py` asserts - so it transcribes at 1:1 into the 20K the MOS puts at `&3000`. The **mode change moves to the front of boot**: it was last because the banks staged through `&4000`, which MODE 2 puts on screen, and they stage in the SHADOW screen now, which nothing is displaying while the picture is up in main. **The picture is two disc files**, because ZX0 unpacks forwards: a stream may share memory with its own output only while the reader stays ahead of the writer, so a stream for the whole screen would have to start at `&8000` minus its own length and its tail would run past `&8000` into the ROM window. It is staged BELOW the screen instead, at `LOAD_STREAM = &2200`, where there are 3,584 bytes against the 4,659 the picture packs to; two halves cost 44 bytes over one. The depacker is Paradroid's, lifted, and is the one thing in the code image allowed above `SPR_SAVE`'s base - it is boot code and is dead before anything reads there. `tools/make_disc.py` compresses, round-trips every stream through `tools/zx0.py`, rewrites the catalogue load addresses and lays the files out in boot order, so **beebasm's own image is not bootable**. 91,904 bytes of files becomes 37,632; measured, it pays for the picture and no more - 11.1 s to 10.9 s | `docs/layer-9-loader.md` |
| 39 | 2026-09-04 | **Q mutes and unmutes the tune** (KC), and **the key is read in the VSync handler**. `IKN_q = 16`, measured with OSBYTE 121 in a BASIC session holding the key, never recalled; Q was free because the C64's paused-game abort is ESCAPE here (decision 32). In the IRQ rather than the main loop because it has to work wherever the foreground is - playing, held in `pause_check`'s blocking loop, on the titles, or watching the finale - and the handler is the only thing that runs in all of them. Safe because the foreground's `keydown` wraps its latch sequence in `php`/`sei`/`plp`, so the IRQ cannot land inside one. Muting runs `sn_reset` **instead of** `vgm_update`, so the tune stops where it is and resumes there. Doing it the other way round - letting the player run and silencing the chip after it, so the tune would play on underneath - crackled on both jsbeeb and b2: the write capture shows `vgm_decode_frame` putting the real volume on channel 2 and `sn_reset` taking it off 246 cycles later, which is 123 us of the tune fifty times a second. No ordering closes that window while the player is still writing volumes (`BUGS.md` #11). Edge-triggered on the press, and the flag lives in the code image so a new game does not unmute | `docs/layer-7-music.md` |
| 40 | 2026-09-04 | **An Arkos tracker replay is ported and wired in behind `MUSIC_AKL`, as a comparison build - the choice between it and the VGI player is OPEN, pending KC's ear** (KC asked for the port and the listening test). `src/aklplayer.asm` is a 6502 port of Arkos Tracker 2's "lightweight" replay and `src/ay2sn.asm` converts AY registers to the SN76489 every frame, replacing `lib/vgiplayer.asm` and its pre-converted register log. The case for it: the tracker data for the WHOLE 349-second tune is 4,741 bytes against 23,514 of `.vgi`, so player, converter and tune together are 7,640 and fit in HAZEL alone - `music_lo` leaves sideways bank 3 and takes 8,960 bytes of it with it, and nothing is truncated. The cost is small and measured: +854 us on the worst frame and nine missed flips against seven, over the same brutal test Layer 7 used. The case against it is not CPU or memory but **sound**: `ym2sn.py` does whole-song analysis a per-frame converter cannot - it picks a priority bass channel and synthesises sub-122 Hz tones with periodic noise, and averages the hardware envelope across each frame, where ours samples it once - so the two agree on only 63.9% of tone periods off the envelope and 3.6% on it. The replay itself is not in doubt: it is byte-exact against Arkos's own player over all 17,446 frames, and twelve fields captured out of the running game match the simulation uniquely. **Two things must be watched if it ships**: AKL encodes only envelope shapes 8 and 0xa and EDGEA is 12 throughout, so `ENV_BASE` compensates and must change if the tune does; and the arpeggio-table, pitch-table, soft-and-hard and five of the seven effect paths are written but have never run, because this song uses none of them | `docs/layer-7-music-arkos.md` |
| 41 | 2026-09-04 | **The Amstrad CPC's artwork is wired in behind `GFX_CPC`, as a comparison build - the choice between it, the C64 conversion and the hand-authored redraw of decision 3 is OPEN, pending KC's eye** (KC asked for the build option). All 119 sprite frames and all 256 background characters come from Trevor "Smila" Storey's CPC art instead of the C64's; nothing else changes, because **the CPC port renumbered nothing** - measured, not assumed: frame for frame the CPC sprite bank matches `assets/sprite-sheet.png`'s opaque mask at offset 0 with 99.9% agreement against 76% for the next best offset, and the CPC tile table is the C64's transposed with the same character numbers. So the tile table, the map, `col_decode`, the waves, `dp_dcd`, the panel, the HUD and the titles are shared and only the `INCBIN` changes. The palette is `Mode0Pal` from `Compiled_Main3.asm`, the in-game one **stored reversed** because `SetColours` walks it from pen 15 down - not the `.PAL` beside the art on the work disc, which differs on pens 13 and 14 and would draw the explosions orange rather than blue. Mode 0's fifteen colours collapse into MODE 2's eight **by hue, not by RGB distance**, which would send pastel yellow, pink, pastel blue and white all to white. Two consequences of sixteen-colour art: transparency is per BYTE, because the CPC masks per byte, which costs 188 bytes across the two sprite banks and still fits; and **the hit flash takes the whole sprite**, since decision 20's tables have no shared blue and white to preserve and no one per-sprite colour to single out. **The compiled bullet is dropped in this build, for want of 13 bytes**: the CPC bullet has fewer see-through bytes so it compiles to 2,860 against 2,652, bank 3 has 195 free below `music_lo`, and the only other way to make room is to cut the tune again - so do not compare frame meters across the two builds | `docs/layer-8a-gfx-cpc.md` |
| 42 | 2026-09-04 | **The status panel is rotated one character column right, to centre it** (KC: "looks like the HUD panel isn't centred"). The C64's 200-byte screen map is 40 columns wide but its art occupies columns 0-37 only, and is exactly mirror-symmetric about that span - measured, by rendering the map's pixels and comparing each scanline with its reverse: symmetric over 0..37, asymmetric over 1..38, 2..39 and 0..39. The two spare columns are eaten by the side borders, because `rout1` sets `$d016 = $17` for the panel raster - 38-column mode, x-scroll 7 - so on the C64 the bar is centred. Our MODE 2 row shows all forty, so the art sat four pixels left of centre. `export_panel.py`'s `PANEL_SHIFT` rotates every row right by one column: the blank columns 38 and 39 become 39 and 0, the art lands on 1-38, and row 4 - `$ff` in all forty - is unchanged by the rotation, so the bar under the panel still runs edge to edge. `hud_cell_lo`/`hud_cell_hi` in `bank3.asm` carry the same `HUD_COL_SHIFT = 1` on the score, high-score and lives columns. Checked after: all forty scanlines of `panel.bin` are their own mirror, and the digits still sit inside their boxes on the titles and in play | `docs/layer-6d-hud.md` |
| 43 | 2026-09-04 | **The tune stops with the pause, and P comes back out of it as well as fire** (KC). **The C64 does neither**: its `jsr $2e03` sits in raster split 2 and runs every frame whatever the foreground is doing, so the original's music plays on through `main_pause`, and only its fire button leaves. The stop reuses Q's mechanism whole - a `music_pause` flag makes the VSync handler take the muted path, running `sn_reset` INSTEAD OF `vgm_update`, so the player is not stepped at all and the tune stands where it is and carries on from there. It has to be that way round and not "silence the chip after the player runs", for the crackle reason decision 39 and `BUGS.md` #11 give. `pause_check` sets the flag after the P-release debounce and clears it on both ways out, the ESCAPE abort included - that path never returns to the routine. **Q is no longer read while paused** (it superseded that part of decision 39): there is nothing left for it to mute, so the handler skips the `keydown` altogether. P leaving the pause needs its own release debounce, or the main loop's test at the top of the next frame finds it still held and pauses again on the spot. Verified in jsbeeb on the SN registers: all four channels at attenuation 15 while held, the game frozen on `char_col`, and - the proof the tune is stopped rather than playing on silently - a 120-field pause and a 60-field pause resume at exactly the same note | `docs/layer-6c-state-machine.md` |
| 44 | 2026-09-04 | **The titles' two zoom bands are hardware-scrolled, one CRTC cycle each, with the display wrap set to 8K and the two bands split across MAIN and SHADOW so they get a ring each - the display bank switching inside the frame** (KC). Four cycles: panel, top band, credits, bottom band, which is the CPC's own shape. The CPC can hardware-scroll because its address counter is masked to ten bits and every scanline wraps inside its own 2K block; ours does not, so a band whose start address moves SHEARS - row `r` gains, at its right edge, the column row `r+1` has just lost. **We already pay that shear every frame in the play area**: `col_copy` writes the incoming byte column into all twenty rows, and an address written for row `r` is written again for row `r-1` exactly 80 steps later, which is the step the display migrates it. So a zoom band is `scroll_advance` at six rows, and the per-frame cost is the C64's own new-column write - about 1,700 cycles a band against ~31,000 for the software shift that was the alternative. The ring is the measured 8K wrap, `&6000-&7FFF`, **one per bank and only one**, which is why the two bands need two banks rather than it being a convenience. Cycles B and C are seven rows with six displayed, and B's blank seventh row - the C64's own blank row above the credits - is where the bank switch goes, so the write has a row of slack. **Six T1 fires, and the sixth is not optional**: R4 must be written inside its own cycle, and with R4 still 6 from cycle C, cycle D never reaches R7 and VSync never happens - built that way first, it hung in `field_wait`. The credits move up a row to the C64's own spacing, which is forced rather than chosen: 6 + 1 + 6 + 1 + 6 is exactly 20 | `docs/layer-6e-titles.md` |
| 45 | 2026-09-04 | **The credits pulse becomes a vertical raster ported from the CPC's `TitleRaster`, with the CPC's own `RasterPal` colours** (KC). The C64's `ttl_pulse` is a HORIZONTAL colour-RAM cycle along the first and last credit lines running in opposite directions, and MODE 2 has no colour RAM; rewriting the glyph pixels would cost ~25,000 cycles a frame. Axelay met the same wall and turned the effect through ninety degrees, so we take that and his colour list with it: sixteen steps of two Gate Array colours - a heat pulse, the body running red, blue, bright red, purple, orange, pink, bright yellow, pastel yellow, bright white and back down with the trailing colour four steps behind - collapsed to MODE 2's eight by hue exactly as decision 41 collapses the artwork. Eight scanlines a line, the two lines indexing the list from opposite ends, one step every two fields as `ttl_pulse_tmr` does. Logical 7 takes the body list because pair 3 is the pair the C64 actually pulses, logical 6 the trailing one, and logical 4 is left as the shadow. **The collapse doubles three of the nine steps** - bright red and orange onto red, pastel yellow onto yellow, pink onto magenta - so the ramp has flat spots the CPC's has not, and whether they are nudged apart is KC's eye | `docs/layer-6e-titles.md` |
| 46 | 2026-09-04 | **The titles page runs at 50 Hz, one zoom cell a field, and is not double buffered.** Both shadow banks are on screen inside the same frame, so there is no hidden bank to draw and nothing to hand over: `field_wait` leaves `frame_ready` alone and the VSync handler never flips. That gives the C64's own rate and granularity unaltered - one cell a 50 Hz frame, and 4 pixels of our 160-pixel screen is the same 2.5% of the width its 8 pixels of 320 is - so decision 23's double tick has nothing to do here. The price is that every write lands in memory that is displayed, so they are scheduled rather than incidental: each band is fetched only during its own cycle, `ttl_frame` runs straight after the VSync handler returns, and it does the top band first because that one has to be finished by absolute row 5 | `docs/layer-6e-titles.md` |
| 47 | 2026-09-04 | **The status panel image leaves sideways bank 3 and becomes a disc file, unpacked straight into each bank's `&3000` at boot** (KC asked for the whole tune; this is where the room came from). It is 3,200 bytes read exactly twice in the life of a run, and it was holding the one address range the tune cannot do without - bank 3's tail, which runs on into HAZEL. `PANEL`'s ZX0 stream is loaded before `MUSIC`, left sitting at `LOAD_STREAM`, and `panel_init` unpacks it once per bank inside `setup_display`. It compresses to 603 bytes, so the disc pays nothing either. Nothing keeps a copy; a repaint would have to re-run the depacker, which is boot code and dead by then | `src/panel.asm`, `docs/layer-7-music.md` |
| 48 | 2026-09-04 | **The whole 349-second tune ships, in FOUR regions of memory, and `lib/vgiplayer.asm` is modified to allow it** (KC: "see if you can find the RAM to get the full VGI tune into the game... don't forget we have ANDY"). This reverses decision 37's truncation to 203 seconds. A `.vgi` is not one blob: it is **eleven independent streams**, one per SN76489 register, and the player reads exactly one byte from each per frame through its own pointer - so each STREAM has to be contiguous and the TUNE does not. `tools/export_music.py` best-fit-decreasing packs the eleven into region A (`&9100-&D2FF`, bank 3's tail running on into HAZEL), **ANDY** (`&8000-&8FFF`, ROMSEL bit 7, measured in jsbeeb - see below), and the tails of sideways banks 1 and 2, and writes `src/data/music_map.asm`: eleven addresses, eleven ROMSEL bytes. The player gains a `VGI_SPLIT` flag - `fetchbyte` writes the stream's ROMSEL byte to `&FE30` before the read (8 cycles, only on the path that touches compressed data at all) and `vgm_stream_mount` reads the map instead of a `.vgi` header. Measured: worst frame 42,083 us against Layer 7's 42,136, and the same 7 missed flips over the same 5,000-field test - the paging costs nothing readable. Verified byte for byte against the reference in jsbeeb at the start of the tune, deep inside it and across the loop point | `docs/layer-7-music.md`, `tools/verify_vgi.py` |
| 49 | 2026-09-04 | **The VGI player's 96 bytes of state live at `&0C00`, the MOS user-font page, and `!BOOT` is assembled at `&2400` rather than in the code image.** Both are consequences of decision 48 needing room in the two tightest places. The player's state has no reason to be in HAZEL beside its code - `fetchbyte` reads it with the sideways window pointed at a music stream, so main RAM is if anything the safer home - and it buys 96 bytes of HAZEL for the tune. `!BOOT` is a disc file the MOS `*EXEC`s, never loaded anywhere and never executed, and it was costing the code image its own 200 bytes of the tightest region in the build; it is assembled in the sprite saves now, the way `src/panel.asm` and `src/loading.asm` assemble theirs. Main RAM went from 111 free to 185 | `src/main.asm` |
| 50 | 2026-09-04 | **The starfield is the CPC port's, because the C64's is dead code** (KC asked for the starfield; that it could not be the original's is the finding). The C64 animates one pixel in row 0 of character `$FF` (`star_decode`, `sta $47f8` in `rout2`) and **character `$FF` is displayed nowhere**: measured, it occurs in none of `tiles.til`'s 3,376 bytes and none of the map's 1,510, its eight bytes in `tiles.chr` are all zero, and the only `$FF` on its screen is the status bar's last row, whose `status_cols` entries are all `$00` - hires colour 0 on a black `$d021`. So `source_cpc/Source/EG_Stars3.asm` is the specification instead: ten stars on scanline 0 of alternating character rows, at the CPC's own byte columns, plotted only into a blank byte so they never cover the scenery and wiped by putting the blank back. A CPC mode 0 row is 80 bytes for 160 pixels, which is ours exactly, so the positions transcribe unaltered. **We do not keep the CPC's per-star address**: `corner_addr` already is it, a star being a sprite that never moves, and `scroll_advance` running after the draw is what holds it still (`BUGS.md` #7) - so the wipe is simply minus one byte column, `corner_addr` moving exactly that between one bank's own draws. Verified at a breakpoint on three consecutive frames spanning both scroll parities and a `corner_addr` advance: the star's offset from the displayed window is 0 every time. **Three deviations from the CPC.** Its clear tests the byte against the star's colour; ours keeps a flag a star a bank, because our background uses all eight colours and a coincidence would leave a black dot on the scenery for six seconds. Its two colours swap every second frame to hide a half-character R3 shift on unsympathetic monitors; we have no R3, and at 25 Hz the swap would be a 12.5 Hz flicker, so it is dropped. Its pens 10 and 4 become MODE 2 logical 6 and 2 - cyan and green, the same two hues under the reversed `Mode0Pal` of decision 41. **Static, it costs 562 us a frame, 1.4%, measured on a matched A/B**, and takes the missed flips over 2,684 frames of parked-ship play from 1 to 18: the stars did not make the frame expensive, but there were eighteen frames sitting inside 1.4% of the line. Decision 51 then made them drift, for 114 us more | `docs/layer-9c-starfield.md` |
| 51 | 2026-09-04 | **The stars drift left, more slowly than the level, at two speeds** (KC: "can we get the stars moving slowly from right to left, slower than the level scrolls, to give the appearance of depth... bonus points if you can have stars at two speeds"). A deviation from BOTH originals: the C64's starfield is dead code (decision 50) and the CPC's stands still on the screen. Each star steps one fat pixel left when `frame_count AND star_mask` is zero - mask 1 for five of them and mask 3 for the other five, against a level that moves one fat pixel a frame, so the two planes run at a half and a quarter of the scenery and **everything is slower than what is in front of it**, which is what makes it read as depth rather than as drift. The two sets are the two the CPC's own table already alternates by row and colour, so cyan becomes the near plane and green the far one and the rows alternate between them. **A step is half a byte**: leaving the LEFT half of a byte lands in the RIGHT half of the one before it, so the pixel shifts down and the column goes back one; leaving the right half lands in the left half of the same byte and only the pixel moves. MODE 2 puts the left pixel at bits 7,5,3,1 and the right at 6,4,2,0, so the two forms are one ASL apart and `AND #&55` tells them apart with no flag. **The wipe changes shape with it**: the static version inferred the previous address as "minus one byte column", which was only true while the star was nailed to the screen, so the address plotted is now stored per star per bank (52 bytes of the `&0800` block, high byte 0 meaning "not plotted") - strictly more robust, nothing is inferred. Stars now visit column 79, the incoming one, which is safe only because `scroll_frame` runs before `star_frame`; the static version's ASSERT keeping them out of it is gone. **114 us on top of the static 562, so 676 us a frame, 1.7%**, and one extra missed flip over 2,684 frames (19 against 18). Verified by reading `star_col` over exactly 100 game passes: every star in a set moved the same amount, the sets are 2:1, and three wrapped the left edge and returned at the right | `docs/layer-9c-starfield.md` |
| 52 | 2026-09-04 | **A memorial to T.M.R. between the loading screen and the titles** (KC). Jason "T.M.R." Kelk wrote *Edge Grinder* and is already in the credits the titles draw; this is the one sequence on the disc that is nobody's port of anything. The loading picture fades out, "IN MEMORY OF T.M.R." fades up in the credits' own font in the middle of the screen, holds three seconds and fades out again into the titles. **The fade is the palette and nothing else**: MODE 2's eight colours sit on one brightness ladder - black, blue, red, magenta, green, cyan, yellow, white - so a step down the ladder is the whole picture one step darker, white -> yellow -> cyan -> green -> magenta -> red -> blue -> black, which is KC's own sequence. Sixteen writes to `&FE21` a rung, eight rungs, six fields a rung, and not one byte of the picture is touched. Fading DOWN subtracts the rung and clamps at black; fading UP caps each colour's rung instead, so the font's blue arrives at rung 1, its cyan at 5 and its white at 7 and the message assembles itself rather than dissolving in. It runs with **interrupts off** - `install_irq` is the next CLI - and times itself off the System VIA's own VSync flag, because the MOS's handler would take that flag before a poll could see it. **It is in two halves**, because the `GFX_CPC` build's bank 3 had 162 bytes below the tune and the whole thing is 275: the drawing needs the font and is in bank 3 (`mem_page`), the fade and the sequencing need nothing and are in main RAM (`memorial`). Paying for it moved the boot loader above `code_end`, where the depacker already was, and moved `LOAD_STREAM` from `&2200` to `&2400` | `docs/layer-9d-memorial.md` |
